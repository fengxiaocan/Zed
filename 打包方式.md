# Zed 安装包构建与打包指南

---

## 🐧 Linux 平台（Debian / Ubuntu / Deepin / UOS 等 .deb 安装包）

### 📁 脚本入口

1. **`build-deb.sh`** —— 根目录下的 Linux Shell 脚本（推荐使用）
2. **`build-deb`** —— 根目录下的快捷软链接（支持直接 `./build-deb`）
3. **`script/build-deb`** —— 底层核心打包逻辑脚本

---

### 🚀 使用方法

#### 1. 一键全自动编译并生成 .deb 安装包

在终端中直接运行：

```bash
./build-deb.sh
# 或
./build-deb
```

#### 2. 常用参数与使用场景

| 场景 | 命令 | 说明 |
| :--- | :--- | :--- |
| **快速二次打包（跳过编译）** | `./build-deb.sh -s` | 若之前已执行过 release 构建，直接提取现有二进制与依赖库打包，秒级完成 |
| **打包并立即安装到系统** | `./build-deb.sh -i` | 打包完成后自动调用 `sudo apt install` 安装并配置全局 `zed` 命令 |
| **快速打包并自动安装** | `./build-deb.sh -s -i` | 结合跳过编译与自动安装，用于本地开发验证 |
| **指定发布渠道** | `./build-deb.sh -c preview` | 支持 `dev`、`preview`、`nightly`、`stable` |
| **指定目标架构** | `./build-deb.sh -a amd64` | 支持 `amd64` / `x86_64`、`arm64` / `aarch64` |
| **指定输出目录** | `./build-deb.sh -o ./dist` | 将生成的 `.deb` 文件存放到指定目录（默认为项目根目录） |
| **跳过许可证生成（加速）** | `./build-deb.sh --skip-licenses` | 免去 `cargo-about` 扫描，加快本地打包速度 |
| **查看完整帮助文档** | `./build-deb.sh -h` | 输出参数说明和使用示例 |

#### 3. 手动安装生成的安装包

```bash
# 使用 apt 安装（推荐，会自动解决依赖关系）：
sudo apt install ./zed_1.15.0_amd64.deb

# 或使用 dpkg 安装：
sudo dpkg -i zed_1.15.0_amd64.deb

# 验证安装包信息：
dpkg -I zed_1.15.0_amd64.deb

# 查看安装包内部文件清单：
dpkg -c zed_1.15.0_amd64.deb
```

---

## 🪟 Windows 平台（Standalone .exe 安装程序）

### 📁 脚本入口

1. **`build-installer.ps1`** —— 根目录下的 PowerShell 脚本（推荐使用）
2. **`build-installer.bat`** —— 根目录下的批处理脚本（支持直接双击运行或 CMD 运行）
3. **`script/bundle-windows.ps1`** —— 核心打包与 Inno Setup 编译逻辑脚本

---

### 🚀 使用方法

#### 1. 一键全自动编译并生成安装包

在 PowerShell 中直接运行：

```powershell
.\build-installer.ps1
```

或在 CMD / 直接双击运行：

```cmd
build-installer.bat
```

#### 2. 常用参数与使用场景

| 场景 | 命令 | 说明 |
| :--- | :--- | :--- |
| **编译并立即运行安装** | `.\build-installer.ps1 -Install` | 打包完成后自动启动生成的 `.exe` 安装程序 |
| **打包后打开所在文件夹** | `.\build-installer.ps1 -OpenFolder` | 打包完成后自动在文件资源管理器中定位到安装包 |
| **快速二次打包（跳过编译）** | `.\build-installer.ps1 -SkipBuild` | 若之前已执行过 `cargo build --release`，可跳过编译直接打包 |
| **指定发布渠道** | `.\build-installer.ps1 -Channel preview` | 支持 `dev`、`preview`、`nightly`、`stable` |
| **跳过许可证生成（加速）** | `.\build-installer.ps1 -SkipLicenses` | 免去 `cargo-about` 扫描，加快本地打包速度 |
| **查看完整帮助文档** | `.\build-installer.ps1 -Help` | 输出参数说明和使用示例 |
