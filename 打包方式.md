 ### 📁 脚本入口

  1. build-installer.ps1 —— 根目录下的 PowerShell 脚本（推荐使用）
  2. build-installer.bat —— 根目录下的批处理脚本（支持直接双击运行或 CMD 运行）
  3. bundle-windows.ps1 —— 核心打包与 Inno Setup 编译逻辑脚本（已全面增强与修复）
  ──────
  ### 🚀 使用方法

  #### 1. 一键全自动编译并生成安装包

  在 PowerShell 中直接运行：

    .\build-installer.ps1

  或在 CMD / 直接双击运行：

    build-installer.bat

  #### 2. 常用参数与使用场景

   场景                     │ 命令			 │ 说明
  ──────────────────────────┼────────────────────────────┼─────────────────────────────
   编译并立即运行安装       │ .\build-installer.ps1 -    │ 打包完成后自动启动生成的
                            │ Install			 │ .exe 安装程序
   打包后打开所在文件夹     │ .\build-installer.ps1 -    │ 打包完成后自动在文件资源管
                            │ OpenFolder		 │ 理器中定位到安装包
   快速二次打包（跳过编译） │ .\build-installer.ps1 -    │ 若之前已执行过 cargo build
                            │ SkipBuild			 │ --release，可跳过编译直接打
                            │                            │ 包
   指定发布渠道             │ .\build-installer.ps1 -    │ 支持
                            │ Channel preview            │ dev、preview、stable、night
                            │                            │ ly
   跳过许可证生成（加速）   │ .\build-installer.ps1 -    │ 免去 cargo-about
                            │ SkipLicenses		 │ 扫描，加快本地打包速度
   查看完整帮助文档         │ .\build-installer.ps1 -    │ 输出参数说明和使用示例
